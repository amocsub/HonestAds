{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Wrangler Dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "npx wrangler dev --config wrangler.toml",
      "cwd": "${workspaceFolder}"
    }
  ]
}
